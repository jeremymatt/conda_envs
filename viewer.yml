name: viewer
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  - spyder
  - numpy
  - pandas
  - pip
  - pillow
  - tqdm
  - pip:                            # Packages installed from PyPI
    - tkFileDialog
    - tkMessageBox
    - tkSimpleDialog

